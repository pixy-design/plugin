---
description: Wire this project for Pixy — pairing, permissions, script tag, verification
---

Load the `pixy` skill (from this plugin) and run its **Setup** flow to
completion: key (pair if needed), project settings + permissions, script tag
installed in the source, dev server verified, `.claude/pixy.json` written.
You do the wiring, never the user; end with the single-restart handoff.
