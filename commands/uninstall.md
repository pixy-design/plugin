---
description: Remove Pixy from this project — script tag, settings entries, state file
---

Load the `pixy` skill (from this plugin) and run its **Uninstall** flow:
remove the script tag, strip the Pixy entries from `.claude/settings.json`,
delete `.claude/pixy.json`, and point the user at the dashboard for key
deletion.
